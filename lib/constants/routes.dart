import 'package:get/get.dart';

class AppRoutes {
  AppRoutes._();
  static const String shell = '/shell';

  static final List<GetPage> pages = [
    //GetPage(name: shell, page: () => const MainShell()),
  ];
}
